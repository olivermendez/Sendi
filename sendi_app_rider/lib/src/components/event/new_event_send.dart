import 'package:flutter/material.dart';

class NewEventToSend extends StatelessWidget {
  //const NewEventToSend({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      width: 500,
      color: Colors.red,
      child: Text("Soy un Container"),
    );
  }
}

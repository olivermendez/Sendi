import 'package:flutter/material.dart';

class RiderDetailsScreen extends StatelessWidget {
  //const RiderDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
